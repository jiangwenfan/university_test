mount #显示所有系统中已经挂载的设备。 mount -l 会显示卷标。
#mount是看不出swap分区的，这个swap分区不需要挂载，使用fdisk -l可以看到。
mount -a #根据配置文件/etc/fstab的内容自动挂载。

mount -t 文件系统 -o 特殊选项 设备文件名 挂载点 #\表示这一行没有写完，换行,下一行继续。

挂载分区:
mkdir /mnt/cdrom
 
挂载U盘
ls /dev/sd* #查看多出来的就是U盘
#fdisk -l |grep GB #查看容量来确定U盘
mount /dev/sdd1 /home/m.2/u_disk
