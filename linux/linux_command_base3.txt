linux base 5
/bin :经常使用的命令
/boot :启动Linux时的核心文件，链接文件和镜像文件
/dev :Linux的外部设备
/etc ：配置文件和子目录
/home :用户主目录
/lib :动态链接共享库===windows的DLL文件
/lost+found :当系统非法关机之后，就会存放一些文件。
/media :自动识别一些设备，并且挂载在这个目录下
/mnt :临时挂载别的文件系统，可以将光驱挂载在/mnt/上，然后进入即可查看文件。
/opt :这是给主机额外安装软件所摆放的目录。
/proc ：虚拟目录，是内存的映射。

/etc/sysconfig/network-scripts/ifcfg-eth0  #网卡配置文件,一律大写
/etc/hosts  #本地DNS解析
/etc/resolv.conf   #DNS配置文件
/etc/apt/sources.list　＃软件源配置文件 　

重启所有网卡：/etc/init.d/network restart
重启eth0网卡：ifdown eth0 && ifup eth0

## linux安全 (防火墙，Selinux)
iptables：
https://blog.csdn.net/cyb_19891129/article/details/46988125
https://blog.csdn.net/terry100000/article/details/80156748
firewalld:
systemctl  status firewalld.services  //查看防火墙状态
systemctl start firewalld.services    //打开防火墙
systemctl stop  firewalld.services    //关闭防火墙
systemctl  disable firewalld.services		//开机关闭防火墙
systemctl  enable  firewalld.services	//开机打开防火墙

Selinux:
getenforce	//查看selinux状态。disabled是关闭
setenforce   0		//临时关闭
vim 	/etc/selinux/config 	//永久关闭，然后在重启。reboot.
SELINUX=disabled


查看selinux的状态：sestatus
临时关闭：setenforce 0

永久关闭：
vim  /etc/selinux/config
      selinux=disabled
查看是否关闭：getenforce

## 进程管理
ps
ps aux  //显示系统所有的进程
ps -l 
top:
top -d 2
killall:
killall -l syslogd  //启动
killall -9 httpd   //杀了
killall -i  -9 bash  //询问杀了
netstat:
-a :所有连接
-t:tcp
-u:udp
-n:不列出服务器名称，以端口号显示。
-l:正在监听的服务。
-p:列出网络服务的pid

## 磁盘管理
df:	列出文件系统的整体磁盘使用量。
du:	检查磁盘空间使用量

fdisk:	用于磁盘分区


df命令参数功能：检查文件系统的磁盘空间占用情况。可以利用该命令来获取磁盘被占用了多少空间，目前还剩下多少空间等信息。
-a :列出所有的文件系统，包括系统特用的/proc等文件系统。
-k :以KBytes的容量显示各文件系统；
-m :以MBytes的容量显示各文件系统；
-h :以人们较易阅读的GBytes,MBytes,KBytes等格式自行显示。
-H :以M=1000K取代M=1024K的进位方式。
-T :显示文件系统类型，连同该partition的filesystem名称(例如ext3)也列出；
-i :不用硬盘容量，而以inode的数量来显示。
eg:
将系统内所有的文件系统列出来：
df
 #如果df没有见任何选项，那么默认会将系统内所有的(不含特殊内存内的文件系统与swap)都 以1Kbytes的容量来列出来。
将容量结果以易读的容量格式显示出来
df -h
将系统内的所有特殊文件格式及名称都列出来
df -aT
将/etc底下的 可用的磁盘容量以易读的容量格式显示
df -h /etc

du命令也是查看使用空间的，但是与df命令不同的是，du命令是对文件和目录磁盘使用的空间查看。
-a :列出所有的文件与目录的容量。默认仅统计目录底下的文件量。
-h :以人们易读的容量格式(G/M)显示。
-s :列出总量而以，不列出每个各别的目录占用量；
-S :不包括子目录下的统计。
-k :以KBytes列出容量显示。
-m :以MBytes列出容量显示。

查看linux架构信息：
查看cpu架构:　　uname -i  或者 arch

查看linux发行版的信息;
旧版本：cat  /etc/release
新版本：cat  /etc/redhat-release
ubuntu: cat  /etc/os-release